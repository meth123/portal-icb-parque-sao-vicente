import { PublicPagePlaceholder } from "@/components/public-page-placeholder";

export default function EventsPage() {
  return (
    <PublicPagePlaceholder
      title="Eventos"
      description="A programação de eventos será adicionada em uma etapa futura."
    />
  );
}
